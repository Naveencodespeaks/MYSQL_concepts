# Drop Table MySQL

use naveendb1;

desc employee;

select * from employee;

drop table employee;

select * from employee; # it will show you error because we have deleted the table or Drop the table

desc employee;  # it will show you error because we have deleted the table or Drop the table